import Layout from "@/components/layouts/Layout";

type Props = {};
const Favorites = (props: Props) => {
  return (
    <Layout title="Favorites pokenons">
      <h1>Favorites</h1>
    </Layout>
  );
};
export default Favorites;
